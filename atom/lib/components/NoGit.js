import React from "react"

export default () => {
	return (
		<div id="no-git">
			<h2>Got Git?</h2>
			<h5>Unfortunately, CodeStream only works with projects controlled by Git. :-(</h5>
		</div>
	)
}
